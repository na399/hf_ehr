# @package _global_

data:
  dataset:
    name: FEMRDataset
    path_to_femr_extract: ${oc.env:DATA_DIR,./data}/femr_extract_v8
    is_debug: True

trainer:
  limit_val_batches: 1
  check_val_every_n_epoch: 1
  max_epochs: 5